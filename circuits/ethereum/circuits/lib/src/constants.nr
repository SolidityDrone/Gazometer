// Global constants used across multiple modules to avoid duplicate imports
pub global SR_MAX_DEPTH_NO_LEAF: u32 = 6;
pub global SR_MAX_LEAF_LEN_M: u32 = 1011;
pub global SR_MAX_VALUE_LEN_M: u32 = 1000;
pub global SR_MAX_PREFIXED_KEY_NIBBLE_LEN: u32 = 8; 